extends ./layout.pug

block content
  include ./includes/navbar.pug
  div.row
    div.col-md-8.col-md-offset-2.Course
      h1 All Courses

      div.list-group
        for video in videos
          a.list-group-item(href='/playlist/' + video.id) #{video.title}
